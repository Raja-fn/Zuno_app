name: zuno
description: A Gen-Z social app
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  image_picker: ^1.0.7
  supabase_flutter: ^2.3.4
  video_player: ^2.4.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
