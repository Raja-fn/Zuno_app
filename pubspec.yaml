name: zuno
description: A Gen-Z social app
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  carousel_slider: ^5.1.1
  flutter:
    sdk: flutter

  geocoding: ^4.0.0
  geolocator: ^14.0.2
  image_cropper: ^11.0.0
  image_picker: ^1.2.1
  supabase_flutter: ^2.3.4
  video_player: ^2.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
