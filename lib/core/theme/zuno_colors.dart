import 'package:flutter/material.dart';

class ZunoColors {
  static const Color background = Color(0xFF0B0F14);
  static const Color surface = Color(0xFF141A22);
  static const Color card = Color(0xFF1B222C);

  static const Color primary = Color(0xFF00E5FF);
  static const Color secondary = Color(0xFF6CFF8D);

  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textMuted = Color(0xFF8B949E);
  static const Color text = textPrimary;
  static const Color muted = textMuted;

  static const Color bg = background;
  static const Color surfaceAlt = surface;
}
